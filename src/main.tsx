import { ChangeEvent, FormEvent, StrictMode, useEffect, useMemo, useRef, useState } from 'react'
import { createRoot } from 'react-dom/client'
import './styles.css'

type Tab = 'calendar' | 'workload' | 'decisions' | 'imports' | 'settings'
type Owner = 'Paolo' | 'Anna' | 'Both' | 'Unassigned'
type Kind = 'event' | 'task'
type Status = 'approved' | 'proposed' | 'done' | 'deferred' | 'rejected'
type Category = 'School' | 'Childcare' | 'Activity' | 'Appointment' | 'Family' | 'Work' | 'Travel' | 'Task' | 'Other'
type Item = { id: string; kind: Kind; title: string; day: number; start?: string; end?: string; owner: Owner; category: Category; status: Status; recurrence: 'one-off' | 'weekly'; people?: string; note?: string; effortMinutes?: number; managed?: boolean; linkedTaskId?: string; metadataVersion?: string }
type WeatherDay = { date: string; max: number; min: number; rain: number; wind: number; code: number }

const DAYS = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']
const DATES = ['Sep 7', 'Sep 8', 'Sep 9', 'Sep 10', 'Sep 11', 'Sep 12', 'Sep 13']
const HOURS = Array.from({ length: 13 }, (_, index) => `${String(index + 7).padStart(2, '0')}:00`)
const CATEGORIES: Category[] = ['School', 'Childcare', 'Activity', 'Appointment', 'Family', 'Work', 'Travel', 'Task', 'Other']
const OWNERS: Owner[] = ['Paolo', 'Anna', 'Both', 'Unassigned']
const WEATHER_ICONS: Record<number, string> = { 0: '☀️', 1: '🌤️', 2: '⛅', 3: '☁️', 45: '🌫️', 48: '🌫️', 51: '🌦️', 53: '🌦️', 55: '🌧️', 61: '🌧️', 63: '🌧️', 65: '🌧️', 71: '🌨️', 73: '🌨️', 75: '❄️', 80: '🌦️', 81: '🌦️', 82: '⛈️', 95: '⛈️' }

const SEED_ITEMS: Item[] = [
  { id: 'dropoff', kind: 'event', title: 'School drop-off', day: 0, start: '08:00', end: '08:30', owner: 'Anna', category: 'School', status: 'approved', recurrence: 'weekly', people: 'Children', managed: true, effortMinutes: 30, metadataVersion: '1.0' },
  { id: 'focus', kind: 'event', title: 'Online · listen only', day: 0, start: '10:00', end: '11:00', owner: 'Paolo', category: 'Work', status: 'approved', recurrence: 'one-off', note: 'Obfuscated work annotation', managed: true, metadataVersion: '1.0' },
  { id: 'pickup', kind: 'event', title: 'School pickup', day: 1, start: '15:30', end: '16:00', owner: 'Unassigned', category: 'Childcare', status: 'proposed', recurrence: 'weekly', people: 'Children', note: 'Needs organizer decision', managed: true, effortMinutes: 30, metadataVersion: '1.0' },
  { id: 'critical', kind: 'event', title: 'Online · critical', day: 1, start: '15:00', end: '16:00', owner: 'Paolo', category: 'Work', status: 'approved', recurrence: 'one-off', note: 'Fixed, active participation', managed: true, metadataVersion: '1.0' },
  { id: 'school-reply', kind: 'task', title: 'Reply to school message', day: 1, owner: 'Paolo', category: 'Task', status: 'approved', recurrence: 'one-off', managed: true, effortMinutes: 15, metadataVersion: '1.0' },
  { id: 'activity', kind: 'event', title: 'Activity drop-off', day: 3, start: '17:00', end: '17:30', owner: 'Anna', category: 'Activity', status: 'approved', recurrence: 'weekly', people: 'Children', managed: true, effortMinutes: 30, metadataVersion: '1.0' },
  { id: 'shopping', kind: 'task', title: 'Add weekend items to list', day: 4, owner: 'Both', category: 'Task', status: 'done', recurrence: 'one-off', managed: true, effortMinutes: 20, metadataVersion: '1.0' },
]
const EMPTY_FORM = { title: '', kind: 'event' as Kind, day: 0, start: '09:00', end: '10:00', owner: 'Unassigned' as Owner, category: 'Family' as Category, recurrence: 'one-off' as 'one-off' | 'weekly', people: '', note: '', linkedTaskId: '' }

function minutes(value?: string) { if (!value) return 0; const [hours, mins] = value.split(':').map(Number); return hours * 60 + mins }
function readLocal<T>(key: string, fallback: T): T { try { const value = localStorage.getItem(key); return value ? JSON.parse(value) as T : fallback } catch { return fallback } }
function notify(setNotice: (value: string) => void, message: string) { setNotice(message); window.setTimeout(() => setNotice(''), 2600) }
function ownerLabel(owner: Owner, names: { paolo: string; anna: string }) { return owner === 'Paolo' ? names.paolo : owner === 'Anna' ? names.anna : owner }

function App() {
  const [items, setItems] = useState<Item[]>(() => readLocal('family-planner-calendar-cache', SEED_ITEMS))
  const [names, setNames] = useState(() => readLocal('family-planner-organizers', { paolo: 'Paolo', anna: 'Anna' }))
  const [tab, setTab] = useState<Tab>('calendar')
  const [quickId, setQuickId] = useState<string | null>(null)
  const [editing, setEditing] = useState<Item | null>(null)
  const [creating, setCreating] = useState(false)
  const [form, setForm] = useState(EMPTY_FORM)
  const [notice, setNotice] = useState('')
  const [weather, setWeather] = useState<WeatherDay[] | null>(null)
  const [weatherState, setWeatherState] = useState<'loading' | 'ready' | 'error'>('loading')
  const fileInput = useRef<HTMLInputElement>(null)

  useEffect(() => { localStorage.setItem('family-planner-calendar-cache', JSON.stringify(items)) }, [items])
  useEffect(() => { localStorage.setItem('family-planner-organizers', JSON.stringify(names)) }, [names])
  useEffect(() => {
    const cached = readLocal<{ expires: number; days: WeatherDay[] } | null>('family-planner-weather', null)
    if (cached && cached.expires > Date.now()) { setWeather(cached.days); setWeatherState('ready'); return }
    fetch('https://api.open-meteo.com/v1/forecast?latitude=55.6761&longitude=12.5683&daily=weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max,wind_speed_10m_max&timezone=Europe%2FCopenhagen&forecast_days=7')
      .then((response) => { if (!response.ok) throw new Error('Weather unavailable'); return response.json() })
      .then((payload) => { const days = payload.daily.time.map((date: string, index: number) => ({ date, max: Math.round(payload.daily.temperature_2m_max[index]), min: Math.round(payload.daily.temperature_2m_min[index]), rain: payload.daily.precipitation_probability_max[index], wind: Math.round(payload.daily.wind_speed_10m_max[index]), code: payload.daily.weather_code[index] })); setWeather(days); setWeatherState('ready'); localStorage.setItem('family-planner-weather', JSON.stringify({ expires: Date.now() + 1800000, days })) })
      .catch(() => setWeatherState('error'))
  }, [])

  const proposals = items.filter((item) => item.status === 'proposed')
  const counts = useMemo(() => OWNERS.reduce((result, owner) => ({ ...result, [owner]: items.filter((item) => item.owner === owner && item.status !== 'rejected').length }), {} as Record<Owner, number>), [items])
  function openCreate(day = 0, start = '09:00', task?: Item) { setEditing(null); setForm(task ? { ...EMPTY_FORM, title: task.title, kind: 'event', day: task.day, owner: task.owner, category: task.category, note: `Converted from task ${task.id}`, linkedTaskId: task.id } : { ...EMPTY_FORM, day, start, end: `${String(Math.min(Number(start.slice(0, 2)) + 1, 20)).padStart(2, '0')}:00` }); setCreating(true) }
  function openEdit(item: Item) { setEditing(item); setForm({ title: item.title, kind: item.kind, day: item.day, start: item.start ?? '09:00', end: item.end ?? '10:00', owner: item.owner, category: item.category, recurrence: item.recurrence, people: item.people ?? '', note: item.note ?? '', linkedTaskId: item.linkedTaskId ?? '' }); setCreating(true) }
  function save(event: FormEvent) { event.preventDefault(); if (!form.title.trim()) return; const next = { ...form, title: form.title.trim(), id: editing?.id ?? `local-${Date.now()}`, status: editing?.status ?? 'approved', managed: true, metadataVersion: '1.0', start: form.kind === 'task' ? undefined : form.start, end: form.kind === 'task' ? undefined : form.end } as Item; setItems((current) => editing ? current.map((item) => item.id === editing.id ? next : item) : [...current, next]); setCreating(false); notify(setNotice, editing ? 'Calendar draft updated.' : 'Added to the calendar draft.') }
  function toggleTask(id: string) { setItems((current) => current.map((item) => item.id === id ? { ...item, status: item.status === 'done' ? 'approved' : 'done' } : item)); notify(setNotice, 'Task confirmation updated.') }
  function chooseOwner(id: string, owner: Owner) { setItems((current) => current.map((item) => item.id === id ? { ...item, owner } : item)); setQuickId(null); notify(setNotice, `Assigned to ${ownerLabel(owner, names)}.`) }
  function importFile(event: ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file) return; const reader = new FileReader(); reader.onload = () => { try { const payload = JSON.parse(String(reader.result)); if (payload.schema_version !== '1.0' || !Array.isArray(payload.proposals)) throw new Error(); const incoming = payload.proposals.map((proposal: any, index: number): Item => ({ id: `proposal-${proposal.external_id}`, kind: proposal.type === 'task' ? 'task' : 'event', title: proposal.title ?? `Imported ${proposal.type}`, day: index % 7, start: proposal.type === 'task' ? undefined : proposal.start?.slice(11, 16) ?? '09:00', end: proposal.type === 'task' ? undefined : proposal.end?.slice(11, 16) ?? '10:00', owner: proposal.suggested_assignee === 'organizer_1' ? 'Paolo' : proposal.suggested_assignee === 'organizer_2' ? 'Anna' : proposal.suggested_assignee === 'both' ? 'Both' : 'Unassigned', category: proposal.type === 'task' ? 'Task' : 'Family', status: 'proposed', recurrence: 'one-off', note: `Imported proposal · ${proposal.source ?? 'unknown'} · ${proposal.reason ?? ''}`, managed: true, metadataVersion: '1.0' })); setItems((current) => [...current, ...incoming.filter((item: Item) => !current.some((old) => old.id === item.id))]); setTab('decisions'); notify(setNotice, `${incoming.length} proposal(s) added to Decisions.`) } catch { notify(setNotice, 'Import rejected: expected Family Planner JSON 1.0.') } }; reader.readAsText(file); event.target.value = '' }
  function reset() { setItems(SEED_ITEMS); notify(setNotice, 'Calendar draft reset.') }

  return <main className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">FP</span><div><strong>Family Planner</strong><small>Shared calendar mode</small></div></div><nav>{(['calendar', 'workload', 'decisions', 'imports', 'settings'] as Tab[]).map((value) => <button className={`nav-button ${tab === value ? 'active' : ''}`} key={value} onClick={() => setTab(value)}><span className="nav-icon">{({ calendar: '▦', workload: '◒', decisions: '◇', imports: '↥', settings: '⚙' } as Record<Tab, string>)[value]}</span><span>{value[0].toUpperCase() + value.slice(1)}</span>{value === 'decisions' && proposals.length > 0 && <b>{proposals.length}</b>}</button>)}</nav><div className="sidebar-bottom"><div className="privacy-note"><span>●</span><div><strong>Calendar is authoritative</strong><small>Local cache only in this demo</small></div></div><button className="link-button" onClick={reset}>Reset demo data</button></div></aside><section className="main-panel"><header className="topbar"><div><span className="eyebrow">WEEK OF 7–13 SEPTEMBER 2026</span><h1>{tab === 'calendar' ? 'Plan the week together.' : ({ workload: 'Balance the load.', decisions: 'Decisions need you.', imports: 'Bring in the week.', settings: 'Make it work for you.' } as Record<string, string>)[tab]}</h1></div><div className="top-actions"><button className="quiet-button" onClick={() => fileInput.current?.click()}>Import JSON</button><button className="primary-button" onClick={() => openCreate()}>＋ Add Event</button><div className="avatar-pair"><span>{names.paolo[0]}</span><span>{names.anna[0]}</span></div></div></header>{notice && <div className="toast" role="status">{notice}</div>}{tab === 'calendar' && <Calendar items={items} names={names} weather={weather} weatherState={weatherState} quickId={quickId} proposals={proposals} onQuick={setQuickId} onOwner={chooseOwner} onToggle={toggleTask} onEdit={openEdit} onCreate={openCreate} onImport={() => fileInput.current?.click()} onExport={() => notify(setNotice, 'Export approved events from the calendar controls.')} />}{tab === 'workload' && <Workload items={items} counts={counts} onEdit={openEdit} onToggle={toggleTask} />}{tab === 'decisions' && <Decisions items={proposals} onEdit={openEdit} onStatus={(id: string, status: Status) => { setItems((current) => current.map((item) => item.id === id ? { ...item, status } : item)); notify(setNotice, status === 'approved' ? 'Proposal approved.' : 'Proposal deferred.') }} />}{tab === 'imports' && <Imports onChoose={() => fileInput.current?.click()} />}{tab === 'settings' && <Settings names={names} onNames={setNames} onReset={reset} />}<input ref={fileInput} className="visually-hidden" type="file" accept="application/json,.json" onChange={importFile} /></section>{creating && <Editor form={form} editing={editing} onForm={setForm} onSave={save} onDelete={() => { if (editing) setItems((current) => current.filter((item) => item.id !== editing.id)); setCreating(false) }} onClose={() => setCreating(false)} />}</main>
}

function Weather({ weather, state }: { weather: WeatherDay[] | null; state: 'loading' | 'ready' | 'error' }) { if (state === 'loading') return <div className="weather-strip"><span className="weather-pin">☁️</span><strong>Copenhagen weather</strong><small>Loading…</small></div>; if (state === 'error' || !weather) return <div className="weather-strip error-weather"><span className="weather-pin">☁️</span><strong>Copenhagen weather unavailable</strong><small>Calendar remains available</small></div>; return <div className="weather-strip"><div className="weather-location"><span className="weather-pin">{WEATHER_ICONS[weather[0].code] ?? '🌥️'}</span><span><strong>Copenhagen area</strong><small>7-day forecast · °C · km/h</small></span></div><div className="weather-days">{weather.map((day) => <div className="weather-day" key={day.date}><strong>{new Intl.DateTimeFormat('en-DK', { weekday: 'short' }).format(new Date(`${day.date}T12:00:00`))}</strong><span>{WEATHER_ICONS[day.code] ?? '🌥️'}</span><b>{day.max}° <small>{day.min}°</small></b><em>{day.rain}% rain</em></div>)}</div></div> }

function QuickActions({ item, names, onOwner, onToggle, onEdit }: any) { return <div className="quick-actions" role="group" aria-label={`Quick actions for ${item.title}`}><span className="quick-label">Quick assignment</span>{OWNERS.map((owner) => <button className={owner === item.owner ? 'selected' : ''} key={owner} onClick={(event) => { event.stopPropagation(); onOwner(item.id, owner) }}>{ownerLabel(owner, names)}</button>)}{item.kind === 'task' && <button onClick={(event) => { event.stopPropagation(); onToggle(item.id) }}>{item.status === 'done' ? 'Reopen task' : 'Confirm done'}</button>}<button onClick={(event) => { event.stopPropagation(); onEdit(item) }}>Edit</button></div> }
function CalendarItem({ item, names, quick, onQuick, onOwner, onToggle, onEdit }: any) { const top = (minutes(item.start) - 420) * 56 / 60; const height = Math.max((minutes(item.end) - minutes(item.start)) * 56 / 60, 45); return <article className={`calendar-item ${item.status} ${item.kind}`} style={{ top, height }} onClick={() => onQuick(item.id)} onDoubleClick={() => onEdit(item)}><button className="item-title" onClick={(event) => { event.stopPropagation(); onQuick(item.id) }}>{item.title}</button><span className="item-time">{item.start}–{item.end}</span><button className={`assignee-chip ${item.owner === 'Unassigned' ? 'unassigned' : ''}`} onClick={(event) => { event.stopPropagation(); onQuick(item.id) }}>{ownerLabel(item.owner, names)}</button>{item.kind === 'task' && <button className={`task-check calendar-task ${item.status === 'done' ? 'done' : ''}`} aria-label="Confirm task complete" onClick={(event) => { event.stopPropagation(); onToggle(item.id) }}>{item.status === 'done' ? '✓' : ''}</button>}{quick && <QuickActions item={item} names={names} onOwner={onOwner} onToggle={onToggle} onEdit={onEdit} />}<span className="edit-hint">Double-click to edit</span></article> }
function TaskRow({ item, names, quick, onQuick, onOwner, onToggle, onEdit, onSchedule }: any) { return <div className={`task-row ${item.status === 'done' ? 'task-done' : ''}`}><button className={`task-check ${item.status === 'done' ? 'done' : ''}`} aria-label="Confirm task complete" onClick={() => onToggle(item.id)}>{item.status === 'done' ? '✓' : ''}</button><button className="task-name" onClick={() => onQuick(item.id)}><strong>{item.title}</strong><small>Due {DATES[item.day]} · {item.category} · {ownerLabel(item.owner, names)}</small></button>{quick && <QuickActions item={item} names={names} onOwner={onOwner} onToggle={onToggle} onEdit={onEdit} />}{item.status !== 'done' && <button className="schedule-button" onClick={() => onSchedule(item.day, '09:00', item)}>Schedule</button>}</div> }
function Calendar({ items, names, weather, weatherState, quickId, proposals, onQuick, onOwner, onToggle, onEdit, onCreate, onImport, onExport }: any) { const timed = items.filter((item: Item) => item.start && item.status !== 'rejected'); const tasks = items.filter((item: Item) => !item.start && item.status !== 'rejected'); return <section><div className="calendar-toolbar"><div className="week-switcher"><button aria-label="Previous week">‹</button><strong>September 7 – 13, 2026</strong><button aria-label="Next week">›</button><button className="today-button">Today</button></div><div className="legend"><span><i className="dot blue" />Suggested</span><span><i className="dot green" />Approved</span><span><i className="dot amber" />Needs decision</span></div></div><Weather weather={weather} state={weatherState} /><div className="calendar-hint">Single-click for quick assignment or task actions · Double-click to edit · Touch users can use the Edit action in quick actions</div><div className="calendar-grid"><div className="time-column"><div className="all-day-label">TIME</div>{HOURS.map((hour) => <span key={hour}>{hour}</span>)}</div>{DAYS.map((day, dayIndex) => <div className="day-column" key={day}><button className="day-header" onClick={() => onCreate(dayIndex)}><strong>{day}</strong><span>{DATES[dayIndex]}</span><em>＋</em></button><div className="day-body">{HOURS.map((hour) => <button className="time-slot" key={hour} aria-label={`Add event ${day} ${hour}`} onClick={() => onCreate(dayIndex, hour)} />)}{timed.filter((item: Item) => item.day === dayIndex).map((item: Item) => <CalendarItem item={item} names={names} quick={quickId === item.id} onQuick={onQuick} onOwner={onOwner} onToggle={onToggle} onEdit={onEdit} key={item.id} />)}</div></div>)}</div><div className="overview-lower"><section className="panel task-panel"><div className="panel-heading"><div><span className="eyebrow">TASK LIST · NO FIXED TIME</span><h3>Things to do</h3></div><button className="quiet-button" onClick={() => onCreate(0, '09:00')}>＋ Add task</button></div>{tasks.length === 0 ? <p className="muted empty-copy">No unscheduled tasks.</p> : tasks.map((task: Item) => <TaskRow item={task} names={names} quick={quickId === task.id} onQuick={onQuick} onOwner={onOwner} onToggle={onToggle} onEdit={onEdit} onSchedule={onCreate} key={task.id} />)}</section><section className="panel decision-panel"><span className="eyebrow amber-text">DECISIONS INBOX</span><h3>{proposals.length} suggested item{proposals.length === 1 ? '' : 's'}</h3><p>AI suggestions remain separate until an organizer approves them.</p><button className="outline-button" onClick={() => onQuick(proposals[0]?.id ?? '')}>Review suggestions →</button><button className="quiet-button export-button" onClick={onExport}>↓ Export approved .ics</button></section></div><div className="sync-cards"><SyncCard title="Shared family calendar" text="Authoritative source · connector planned" onClick={() => notify(() => {}, 'Calendar connector is not configured in this demo.')} /><SyncCard title="Work-calendar export" text="Export approved subsets" onClick={onExport} /><SyncCard title="Import proposals" text="JSON file · review first" onClick={onImport} /></div></section> }
function SyncCard({ title, text, onClick }: { title: string; text: string; onClick: () => void }) { return <button className="sync-card" onClick={onClick}><span className="sync-icon">↗</span><span><strong>{title}</strong><small>{text}</small></span><span>→</span></button> }
function Workload({ items, counts, onEdit, onToggle }: any) { const active = items.filter((item: Item) => item.status !== 'rejected'); const total = (owner: Owner) => active.filter((item: Item) => item.owner === owner).reduce((sum: number, item: Item) => sum + (item.effortMinutes ?? Math.max(minutes(item.end) - minutes(item.start), 30)), 0); const chart = (owner: Owner) => { const values = CATEGORIES.map((category) => active.filter((item: Item) => item.owner === owner && item.category === category).reduce((sum: number, item: Item) => sum + (item.effortMinutes ?? 30), 0)); const denominator = values.reduce((sum, value) => sum + value, 0) || 1; let cursor = 0; return values.map((value, index) => { const start = cursor; cursor += value / denominator * 360; return `${['#5c9bc2', '#d49754', '#70a896', '#8d79ba', '#9ba9b6', '#d1b467', '#7d9b8e', '#c47d78', '#a29bb8'][index]} ${start}deg ${cursor}deg` }).join(', ') }; return <section className="content-view"><div className="view-intro"><div><span className="eyebrow">BALANCE OVERVIEW</span><p>Scheduled time and household contribution are separate indicators, not a complete measure of fairness.</p></div><button className="quiet-button">This week ▾</button></div><div className="balance-grid"><article className="balance-card"><span className="eyebrow">SCHEDULED TIME</span><h3>Time by organizer and category</h3><div className="pie-row">{(['Paolo', 'Anna'] as Owner[]).map((owner) => <div className="pie-stat" key={owner}><div className="donut" style={{ background: `conic-gradient(${chart(owner)})` }}><span>{total(owner)}m</span></div><strong>{owner}</strong><small>{counts[owner]} assigned items</small></div>)}</div><div className="category-legend">{CATEGORIES.slice(0, 6).map((category, index) => <span key={category}><i style={{ background: ['#5c9bc2', '#d49754', '#70a896', '#8d79ba', '#9ba9b6', '#d1b467'][index] }} />{category}</span>)}</div></article><article className="balance-card"><span className="eyebrow">HOUSEHOLD CONTRIBUTION</span><h3>Effort allocation</h3><div className="contribution-bars"><Contribution label="Paolo" value={total('Paolo')} color="#5c9bc2" /><Contribution label="Anna" value={total('Anna')} color="#d49754" /><Contribution label="Shared · split 50/50" value={total('Both')} color="#70a896" /><Contribution label="Unassigned" value={total('Unassigned')} color="#b8c1cb" /></div><p className="balance-caveat">Shared items split 50/50 by default. Duration does not capture emotional labor or actual effort.</p></article></div><div className="panel-table">{active.map((item: Item) => <div className="table-row" key={item.id}><span className="category-dot" /><button className="row-link" onClick={() => onEdit(item)}><strong>{item.title}</strong><small>{item.start ? `${DAYS[item.day]} · ${item.start}` : `Unscheduled · ${DATES[item.day]}`} · {item.category}</small></button>{item.kind === 'task' && <button className={`task-check inline ${item.status === 'done' ? 'done' : ''}`} onClick={() => onToggle(item.id)}>{item.status === 'done' ? '✓' : ''}</button>}<span className={item.owner === 'Unassigned' ? 'row-warning' : 'row-assignee'}>{item.owner}</span></div>)}</div></section> }
function Contribution({ label, value, color }: { label: string; value: number; color: string }) { return <div className="contribution"><div><strong>{label}</strong><span>{value} min</span></div><div className="contribution-track"><i style={{ width: `${Math.min(value / 3, 100)}%`, background: color }} /></div></div> }
function Decisions({ items, onEdit, onStatus }: any) { return <section className="content-view"><div className="view-intro"><div><span className="eyebrow amber-text">AI PROPOSALS</span><p>Review each change. Existing calendar duties are never replaced automatically.</p></div><span className="status-count">{items.length} pending</span></div>{items.length === 0 ? <div className="empty-state"><strong>Nothing needs a decision.</strong><span>New imports appear here.</span></div> : <div className="proposal-list">{items.map((item: Item) => <article className="proposal-card" key={item.id}><div><span className="proposal-type">{item.category} · {item.owner}</span><h3>{item.title}</h3><p>{DATES[item.day]} · {item.start ?? 'No fixed time'} · {item.note ?? 'Suggested item'}</p></div><div className="proposal-actions"><button className="approve-button" onClick={() => onStatus(item.id, 'approved')}>Approve</button><button className="edit-button" onClick={() => onEdit(item)}>Edit</button><button className="reject-button" onClick={() => onStatus(item.id, 'deferred')}>Defer</button></div></article>)}</div>}</section> }
function Imports({ onChoose }: { onChoose: () => void }) { return <section className="content-view"><div className="import-hero"><div><span className="eyebrow">JSON IMPORT</span><h2>Bring in a condensed week.</h2><p>Import version 1.0 proposals. They appear in Decisions and remain separate until approved.</p><button className="primary-button" onClick={onChoose}>Choose JSON file</button></div><div className="file-icon">{`{ }`}</div></div><div className="info-callout"><strong>Calendar authority</strong><span>Accepted events belong in the shared family calendar. The app uses local cache only in this demo and never stores raw source calendars.</span></div></section> }
function Settings({ names, onNames, onReset }: any) { return <section className="content-view settings-view"><div className="settings-card"><span className="eyebrow">ORGANIZERS</span><h3>Use the names you prefer</h3><label>First organizer<input value={names.paolo} onChange={(event) => onNames({ ...names, paolo: event.target.value })} /></label><label>Second organizer<input value={names.anna} onChange={(event) => onNames({ ...names, anna: event.target.value })} /></label><p className="settings-note">Defaults are Paolo and Anna. Names are display labels and are stored in calendar metadata when a provider connector is configured.</p><button className="quiet-button" onClick={onReset}>Reset demo</button></div><div className="settings-card"><span className="eyebrow">PLANNING OPERATING MODEL</span><h3>How much should ChatGPT plan?</h3><label>Mode<select defaultValue="weekly"><option>Weekly Review</option><option>Incremental Update</option><option>Urgent Change</option></select></label><label>Planning horizon<select defaultValue="7"><option>7 days</option><option>14 days</option><option>21 days</option><option>28 days</option></select></label><ul className="mode-list"><li><strong>Weekly Review</strong><span>Once weekly, reconcile the chosen horizon.</span></li><li><strong>Incremental Update</strong><span>New event: propose only affected deltas.</span></li><li><strong>Urgent Change</strong><span>Same-day conflict: targeted decision only.</span></li></ul></div></section> }
function Editor({ form, editing, onForm, onSave, onDelete, onClose }: any) { const update = (key: string, value: any) => onForm({ ...form, [key]: value }); return <div className="modal-backdrop" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><form className="edit-dialog" onSubmit={onSave}><div className="dialog-header"><div><span className="eyebrow">{editing ? 'EDIT ITEM' : 'ADD EVENT'}</span><h2>{editing ? 'Change the plan' : 'Add to the calendar'}</h2></div><button type="button" className="close-button" onClick={onClose}>×</button></div><div className="form-grid"><label className="full-width">Title<input autoFocus value={form.title} onChange={(event) => update('title', event.target.value)} placeholder="e.g. School pickup" /></label><label>Type<select value={form.kind} onChange={(event) => update('kind', event.target.value)}><option value="event">Event</option><option value="task">Task without fixed time</option></select></label><label>Category<select value={form.category} onChange={(event) => update('category', event.target.value)}>{CATEGORIES.map((category) => <option key={category}>{category}</option>)}</select></label><label>Day<select value={form.day} onChange={(event) => update('day', Number(event.target.value))}>{DAYS.map((day, index) => <option value={index} key={day}>{day} · {DATES[index]}</option>)}</select></label><label>Owner<select value={form.owner} onChange={(event) => update('owner', event.target.value)}>{OWNERS.map((owner) => <option key={owner}>{owner}</option>)}</select></label><label>Starts<input type="time" value={form.start} onChange={(event) => update('start', event.target.value)} disabled={form.kind === 'task'} /></label><label>Ends<input type="time" value={form.end} onChange={(event) => update('end', event.target.value)} disabled={form.kind === 'task'} /></label><label>Repeats<select value={form.recurrence} onChange={(event) => update('recurrence', event.target.value)}><option value="one-off">One-off</option><option value="weekly">Every week</option></select></label><label className="full-width">People involved<input value={form.people} onChange={(event) => update('people', event.target.value)} placeholder="Optional" /></label><label className="full-width">Note / JSON metadata context<input value={form.note} onChange={(event) => update('note', event.target.value)} placeholder="Optional context" /></label></div><div className="dialog-footer">{editing ? <button type="button" className="delete-button" onClick={onDelete}>Delete draft</button> : <span /> }<div><button type="button" className="quiet-button" onClick={onClose}>Cancel</button><button className="primary-button">{editing ? 'Save changes' : 'Add Event'}</button></div></div></form></div> }

createRoot(document.getElementById('root')!).render(<StrictMode><App /></StrictMode>)
